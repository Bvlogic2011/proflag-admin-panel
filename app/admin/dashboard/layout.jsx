import React from 'react'
export const metadata = {
  title: "Dashboard",
  description: "List of all registered accounts in proflag.",
};


export default function DashboardLayout({ children }) {
  return (
    <>
      {children}
    </>
  )
}
