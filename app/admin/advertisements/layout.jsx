import React from 'react'
export const metadata = {
    title: "Advertisements",
    description: "List of all advertisement in proflag.",
};


export default function AdvertisementsLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}
