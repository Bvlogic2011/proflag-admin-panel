import React from 'react'
export const metadata = {
  title: "Login",
  description: "Login to proflag admin panel.",
};


export default function LoginLayout({ children }) {
  return (
    <>
      {children}
    </>
  )
}
